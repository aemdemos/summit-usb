/**
 * Workspace-relative root for footer validation artifacts (JSON, scripts copy, debug.log).
 * Must match `VALIDATION_DIR` in `hooks/footer-validation-gates/helpers.js`.
 */
export const VALIDATION_DIR = 'migration-work/footer-validation';
